/* @lazyspec (remove to manage manually) */
/* eslint-disable */
import InvestBucket from '../InvestBucket.jsx';

describe('InvestBucket', () => {
  it('exists', () => {
    expect(InvestBucket).toBeTruthy();
  });
});
