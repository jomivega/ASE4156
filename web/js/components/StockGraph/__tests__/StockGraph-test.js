/* @lazyspec (remove to manage manually) */
/* eslint-disable */
import StockGraph from '../StockGraph.jsx';

describe('StockGraph', () => {
  it('exists', () => {
    expect(StockGraph).toBeTruthy();
  });
});
